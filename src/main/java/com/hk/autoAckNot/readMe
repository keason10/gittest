rabbitmq 如果有 100条数据， 4个consummer ,provider 生产速度很快，那么每个consummer就会被分25 个数据处理。
在自动确认的情况下，如果其中一个consummer异常，但是他的25个任务没完全处理完，在自动确认的情况下就会丢失待处理的数据。
但是在手动确认时，处理一条确认一条，防止丢失，如果其中的一个25的consummer异常，只处理了部分任务，则会把没处理的其他任务分配给其他3个consumer，不会丢失数据。
